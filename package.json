{
  "name": "ui-automation-cypress-typescript-bdd",
  "version": "1.0.0",
  "description": "Cypress TypeScript BDD UI automation framework for Sauce Demo.",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "cypress run --browser chrome --env tags=\"not @slow and not @extended and not @bug\"",
    "test:all": "cypress run --browser chrome",
    "test:bugs": "cypress run --browser chrome --env tags=\"@bug\"",
    "test:extended": "cypress run --browser chrome --env tags=\"@extended\"",
    "test:slow": "cypress run --browser chrome --env tags=\"@slow\"",
    "test:headed": "cypress run --headed --browser chrome --env tags=\"not @slow and not @extended and not @bug\"",
    "test:chrome": "cypress run --browser chrome --env tags=\"not @slow and not @extended and not @bug\"",
    "test:report": "cypress run --browser chrome --env tags=\"not @slow and not @extended and not @bug\" --reporter mocha-junit-reporter --reporter-options mochaFile=results/junit/results.xml",
    "validate": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^16.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.8",
    "cypress": "^12.11.0",
    "esbuild": "^0.19.0",
    "mocha-junit-reporter": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
